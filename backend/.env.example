# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration
DB_HOST=localhost
DB_USER=jecr_app
DB_PASSWORD=app
DB_NAME=jecr_app
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=30d

# API Configuration
API_PREFIX=/api
ALLOWED_ORIGINS=https://jecrcfoundation.live

# Email Configuration for OTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=STARTTLS
SMTP_USER=jecrc@jecrcfoundation.live
SMTP_PASSWORD=bhrx xxbl utna scla
EMAIL_FROM=jecrc@jecrcfoundation.live
EMAIL_FROM_NAME=JF Foundation

# OTP Configuration
OTP_EXPIRY_MINUTES=10
SESSION_EXPIRY_DAYS=30

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
